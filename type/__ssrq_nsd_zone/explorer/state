zone_name=${__object_id:?}

nsd-control status >/dev/null 2>&1 || {
	echo 'nsd-control is required' >&2
	exit 1
}

if zone_status=$(nsd-control zonestatus "${zone_name}")
then
	# XXX: check me
	echo present
elif echo "${zone_status}" | grep -q 'error zone .* not configured'
then
	echo absent
fi
